import React, { Fragment } from 'react';
import styled from 'styled-components';

const title = () => {
  return (
    <Fragment>
      <Title>ここから各タイトルを送るよ</Title>
    </Fragment>
  );
};

const Title = styled.h1``;

export default title;
