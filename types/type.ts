// title
export type TitleProps = {
  title: string;
};

// star icon
export type StarProps = {
  color: string;
  fill: string;
};

// skill
export type Skill = {
  language: string;
  img: string;
  star: number;
  description: string;
};

// experinence
export type Experience = {
  period: string;
  role: string;
  description: string;
};
